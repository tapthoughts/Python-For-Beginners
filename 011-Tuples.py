# Tuples can't be changed or modified once created
# Tuples are generally used for data that will not be needing any changes
coordinates = (6, 5, 7, 9, 8)
print(coordinates[3])
print(coordinates)
print("")


# Whereas arrays can be always changed though
abc = [4, 6, 7, 8, 9]
abc[0] = 2
print(abc)

